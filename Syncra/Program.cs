namespace Syncra;

public class Program
{
    public static void Main(string[] args)
    {
        /*
         For the app launch arguments, they'll probably look something like this:

           -<driverName>:<setting> <value>
           For config related to the engine itself, we could use something like this:

           -engine:<setting> <value>
         */

        // separate launch arguments per driver

        // initialize driver processes, passing in arguments

        // initialize local world process; use daemon to keep it alive?
    }
}